import { CalcMembersPipe } from './calc-members.pipe';

describe('CalcMembersPipe', () => {
  it('create an instance', () => {
    const pipe = new CalcMembersPipe();
    expect(pipe).toBeTruthy();
  });
});
